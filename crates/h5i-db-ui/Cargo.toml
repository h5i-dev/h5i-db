[package]
name = "h5i-db-ui"
description = "Local review UI for h5i-db: versions, snapshots, and mutation-plan approval"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
h5i-db-core = { workspace = true }
h5i-db-query = { workspace = true }
arrow = { workspace = true }
axum = "0.8"
futures = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
base64 = { workspace = true }
tower = { version = "0.5", features = ["limit"] }
# Held at 0.6 to stay unified with the copy reqwest pulls in; we use one
# TraceLayer, so 0.7 would buy a second copy and nothing else.
tower-http = { version = "0.6", features = ["trace"] }

[dev-dependencies]
tempfile = { workspace = true }
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
